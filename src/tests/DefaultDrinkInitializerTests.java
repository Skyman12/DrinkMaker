package tests;

import static org.junit.Assert.assertEquals;

import org.junit.Test;

import drinkManager.DrinkManager;
import helperClasses.RotationHelper;
import states.DirectionState;
import states.DrinkLayoutState;

public class DefaultDrinkInitializerTests {
	
	@Test
	public void moveForwardFromStateStart() {
		DrinkManager drinkManager = new DrinkManager(1);
		
    }
}
